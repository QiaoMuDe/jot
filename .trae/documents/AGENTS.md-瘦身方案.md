# AGENTS.md 瘦身方案

## 现状分析

`AGENTS.md` 共 \~2897 行，约 435KB，结构如下：

| 章节                         | 行号           | 说明                                           |
| -------------------------- | ------------ | -------------------------------------------- |
| 一 \~ 九（核心分析报告）             | L1\~L612     | 目录结构、核心模块、依赖关系、设计模式、技术栈、补充分析、核心特点、待优化点、关键记忆点 |
| 十 \~ 一百八十八（旧记忆点）           | L614\~L2773  | 179 个零散的新增记忆点章节，占据文件主体                       |
| 一百八十九 \~ 一百九十八（最近 10 个记忆点） | L2775\~L2897 | 最近 10 个新增记忆点                                 |

## 保留方案

### 保留的内容（不动）

* **一 \~ 九**（L1\~L612）：项目核心分析报告，保持完整

* **一百八十九 \~ 一百九十八**（L2775\~L2897）：最近 10 个新增记忆点

### 移除的内容

* **十 \~ 一百八十八**（L614\~L2773）：179 个旧的新增记忆点章节，全部删除

### 新增的内容

* 在文件末尾追加 **维护规范**，说明只保留最近 10 个记忆点的规则

## 具体修改

### 操作 1：删除 L614\~L2773

删除从 `## 十、新增记忆点（CodeMirror 6 集成）` 到 `## 一百八十八、新增记忆点（AI 聊天消息区滚动条贴右侧边框）` 之间的所有内容（含末尾的 `---` 分隔线）。

### 操作 2：在 L2774 处（一百八十八之后、一百八十九之前）插入分隔线

保留 `---` 分隔线作为过渡。

### 操作 3：在文件末尾追加维护规范

在 L2897（文件末尾）追加以下内容：

```
---

## 十、AGENTS.md 维护规范

1. **第 1-9 章反映项目当前状态**，代码发生结构性变化时更新（新增模块/架构重构图/重要功能/文件行数统计等）
2. **新增记忆点只保留最近 10 个**（即 `一百八十九` ~ `一百九十八`），每次新增一个记忆点时，删除最早的一个（例如新增 `一百九十九` 时，删除 `一百八十九`）
3. **不要无序追加"新增记忆点"章节**——保持编号连续，超出 10 个时执行"先进先出"淘汰
4. **详细的变更记录请写在 `.trae/specs/` 或 `.trae/documents/` 中**，AGENTS.md 仅作为快速参考
5. **更新关键文件统计**时，用 `Measure-Object -Line`（Windows）或 `wc -l`（Linux/macOS）获取实际行数
6. **第 八 章"待优化点"** 中的"已实现"列表仅在重大功能完成时归档，小修改不必追加条目
```

## 预期结果

瘦身后的文件大小：约 612 行（一\~九）+ 122 行（最近 10 个记忆点）+ 约 20 行（维护规范）= **\~754 行，约 110KB**

相比原文件 **2897 行 / 435KB**，缩减约 **74%**。

## 与上一版瘦身的区别

上一版（`AGENTS - 副本.md`）将文件压缩到约 884 行，但内容过于简略：

* 删除了大量一\~九章的细节描述（如依赖关系图、设计模式、技术栈评价等）

* 新增记忆点全部删除，改用"开发记录指引"替代

* 丧失了项目核心知识

本方案：

* **保留一\~九章的完整内容**，不删减核心分析

* **保留最近 10 个记忆点**，保持对近期工作的快速参考

* **添加明确的维护规范**，防止文件再次膨胀

