# Tasks

- [x] Task 1: 修改消息列表响应式宽度 - 将 `.ai-chat-messages` 的 `max-width: 900px` 替换为 `clamp(600px, min(75vw, 100%), 1100px)`，使其随窗口宽度弹性缩放
- [x] Task 2: 修改输入区宽度对齐 - 为 `.ai-chat-input-area` 增加与消息列表一致的响应式宽度约束

# Task Dependencies
- [Task 2] depends on [Task 1]
