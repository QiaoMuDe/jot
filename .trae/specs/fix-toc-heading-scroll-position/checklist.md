# Checkpoints

- [x] `.md-rendered` 的 CSS 规则中包含 `scroll-padding-top` 属性
- [ ] 预览模式下点击 TOC 中的 H1~H6 标题，标题上方有约 `0.75rem` 的留白空间，不紧贴顶部边缘
- [ ] 重复点击同一标题项，定位位置一致、无偏移
- [ ] 不同标题层级（H1~H6）的定位精度一致
- [ ] 浅色/深色主题下定位行为一致（`scroll-padding-top` 不受主题影响）
