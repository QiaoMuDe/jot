# Tasks

- [x] Task 1: 后端新增 `ImportFile` 方法 — 接收文件路径数组，逐个处理：校验（大小/目录）、读取内容、提取标题和类型、创建笔记
- [x] Task 2: 前端注册 `OnFileDrop` 回调 — 调用后端 `ImportFile`，处理成功/失败通知，打开编辑器显示最后创建的笔记

# Task Dependencies

- [Task 2] depends on [Task 1]
