# Tasks

- [ ] Task 1: 在 `variables.css` 中更新 default 主题配色（陈年纸本风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--text-tertiary` / `--scrollbar-thumb` / `--scrollbar-thumb-hover`

- [ ] Task 2: 在 `variables.css` 中更新 light 主题配色（雪光风格）
  - 修改 `--bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--text-secondary` / `--text-tertiary` / `--scrollbar-thumb`

- [ ] Task 3: 在 `variables.css` 中更新 nord 主题配色（极光黎明风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--text-secondary` / `--text-tertiary`

- [ ] Task 4: 在 `variables.css` 中更新 gruvbox-light 主题配色（泛黄报纸风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--text-muted` / `--text-tertiary`

- [ ] Task 5: 在 `variables.css` 中更新 one-dark-pro 主题配色（仅微调辅助色）
  - 修改 `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg`

- [ ] Task 6: 在 `variables.css` 中更新 quiet-light 主题配色（灰玫瑰风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--text-tertiary`

- [ ] Task 7: 在 `variables.css` 中更新 ysgrifennwr 主题配色（羊皮纸风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg`

- [ ] Task 8: 在 `variables.css` 中更新 tokyo-night 主题配色（仅微调辅助色）
  - 修改 `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg`

- [ ] Task 9: 在 `variables.css` 中更新 eye-protection 主题配色（彻底重做，柔和米绿风格）
  - 修改所有颜色变量：`--bg` / `--card-bg` / `--bg-secondary` / `--accent` 系列 / `--text-primary` / `--text-secondary` / `--text-muted` / `--text-tertiary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--success` / `--overlay-bg` / `--toast-bg` / `--toast-text`

- [ ] Task 10: 在 `variables.css` 中更新 dark 主题配色（柔黑风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--text-primary` / `--text-secondary` / `--text-muted` / `--text-tertiary`

- [ ] Task 11: 在 `variables.css` 中更新 dracula 主题配色（仅微调辅助色）
  - 修改 `--bg` / `--bg-secondary` / `--accent-light` / `--accent-lighter` / `--hover-bg` / `--input-bg` / `--text-tertiary`

- [ ] Task 12: 在 `variables.css` 中更新 catppuccin-latte 主题配色（热拿铁风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--text-primary` / `--text-secondary` / `--text-muted` / `--text-tertiary`

- [ ] Task 13: 在 `variables.css` 中更新 alice 主题配色（暖米蓝风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--text-tertiary`

- [ ] Task 14: 在 `variables.css` 中更新 lightmind 主题配色（竹纸风格）
  - 修改 `--bg` / `--card-bg` / `--bg-secondary` / `--accent` / `--accent-dark` / `--border` / `--divider` / `--hover-bg` / `--input-bg` / `--topbar-bg` / `--text-tertiary`

- [ ] Task 15: 构建验证
  - 运行 `npx vite build` 确认构建通过，零错误
  - 确认无 CSS 变量遗漏

# Task Dependencies

- Task 1 ~ Task 14 无依赖关系，可并行执行（均修改同一文件 `variables.css` 的不同区块）
- Task 15 依赖所有前置任务完成
