# Checklist

- [x] `.editor-panel.fullscreen` 的 `height` 改为 `calc(100vh - 56px)`，不覆盖 `#topbar`
- [x] `.editor-overlay.fullscreening` 的 `top` 改为 `56px`，不遮挡 `#topbar`
- [x] 全屏过渡动画平滑自然，无闪烁或跳跃
- [x] 查看笔记页面全屏时 `#topbar` 可见，窗口控制按钮可交互
- [x] 新建笔记页面全屏时 `#topbar` 可见，窗口控制按钮可交互
- [x] 退出全屏后编辑器恢复至原始位置和尺寸
