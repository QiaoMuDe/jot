# Checklist

- [x] 更多菜单中不再直接显示「快捷键说明」和「MD 语法」两个独立菜单项
- [x] 更多菜单中新增一个「帮助参考」子菜单触发器项，带右侧箭头图标
- [x] 鼠标悬停在子菜单触发器上时，展开显示子菜单，内含「快捷键说明」和「MD 语法」两个入口
- [x] 点击子菜单中的「快捷键说明」正确调用 `openShortcuts()`
- [x] 点击子菜单中的「MD 语法」正确调用 `switchView('md-ref')`
- [x] 子菜单位置正确，不超出菜单边界
- [x] 子菜单样式（圆角、阴影、动画）与主菜单风格一致
- [x] 移除 `handleKeyboardNavigation` 中 `Ctrl+7` 和 `Ctrl+8` 的处理逻辑
- [x] 快捷键说明页面中 `Ctrl+7` 和 `Ctrl+8` 条目已移除或标注无快捷键
- [x] `Ctrl+7` 和 `Ctrl+8` 不再触发任何功能
