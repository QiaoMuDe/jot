# Checklist

- [x] 停止生成 — 后端：`CallAIStream` 支持 context 取消，读取循环中检查 `ctx.Done()`
- [x] 停止生成 — 后端：`App` 结构体保存 `cancelFunc`，`CancelAIStream` 绑定方法可取消流
- [x] 停止生成 — 前端：停止按钮在流式开始时显示、结束时隐藏
- [x] 停止生成 — 前端：点击停止按钮取消流式请求，已接收内容保留
- [x] 停止生成 — 前端：停止后恢复发送按钮、输入框可正常使用
- [x] 对话搜索 — HTML：侧栏头部新增搜索输入框
- [x] 对话搜索 — CSS：搜索框样式与布局正确
- [x] 对话搜索 — 逻辑：输入关键词实时过滤会话列表（标题、忽略大小写）
- [x] 对话搜索 — 逻辑：清空搜索框恢复显示所有会话
- [x] 对话搜索 — 逻辑：匹配关键词高亮显示
- [x] 对话搜索 — 逻辑：新建会话后搜索框状态不变