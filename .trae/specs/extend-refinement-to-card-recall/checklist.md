- [ ] 仅启用卡片召回时，搜索词精炼动画正常展示（需运行时验证）
- [ ] 仅启用卡片召回时，精炼后的关键词在前端下拉中可见（需运行时验证）
- [ ] 仅启用卡片召回时，卡片召回结果正常展示（需运行时验证）
- [ ] 联网搜索 + 卡片召回同时启用时，精炼只执行一次（需运行时验证）
- [ ] 联网搜索 + 卡片召回同时启用时，精炼关键词同时用于搜索和召回（需运行时验证）
- [ ] 仅启用联网搜索时，行为与改动前一致（需运行时验证）
- [ ] 两者都关闭时，无精炼动画、无额外 AI 调用（需运行时验证）
- [ ] 精炼失败时，正确发射 `ai:stream-error` 并终止流程（需运行时验证）
- [ ] `CallAIStreamRegenerate` 同步支持以上所有场景（代码审查通过）
- [x] 项目可正常编译通过
