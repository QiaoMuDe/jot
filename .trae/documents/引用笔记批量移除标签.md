# 引用笔记批量移除标签计划

## 当前状态

引用笔记选择器选中 3 篇以上笔记并确认后，每篇笔记各生成一个 chip（`ai-chat-ref-chip`），每个 chip 带单独 X 按钮可逐个移除。没有批量移除机制。

## 目标

当 `referencedNotes.length >= 3` 时，在 chips 末尾追加一个"批量移除"标签，点击一键移除全部引用笔记。

## 修改清单

### 1. `frontend/src/js/ai-chat.js` — `updateRefChips()` 函数

**位置**: 第 3882-3915 行

**修改内容**: 在渲染完每篇笔记的 chip 后，如果 `referencedNotes.length >= 3`，在 `.innerHTML` 末尾追加一个批量移除 chip。

```javascript
function updateRefChips() {
    if (!refChips || !refBar || !refBtn) return;

    if (referencedNotes.length === 0) {
        refBar.style.display = 'none';
        refBtn.classList.remove('has-ref');
        return;
    }

    refBar.style.display = '';
    refBtn.classList.add('has-ref');

    // 渲染单个笔记 chips
    let html = referencedNotes.map(n => {
        const title = n.title || '无标题';
        const truncTip = n.truncated ? '<span class="ai-chat-ref-chip-trunc">(内容已截断)</span>' : '';
        return `<div class="ai-chat-ref-chip" data-id="${n.id}">
            <span class="ai-chat-ref-chip-icon">${DOC_ICON}</span>
            <span class="ai-chat-ref-chip-title" title="${title}">${title}</span>
            ${truncTip}
            <button class="ai-chat-ref-chip-remove" data-id="${n.id}" title="移除引用">
                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
        </div>`;
    }).join('');

    // 3 篇以上时追加批量移除标签
    if (referencedNotes.length >= 3) {
        html += `<div class="ai-chat-ref-chip ai-chat-ref-chip-remove-all" title="一键移除全部引用">
            <svg class="ai-chat-ref-chip-remove-all-icon" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
            <span>移除全部 ${referencedNotes.length} 篇</span>
        </div>`;
    }

    refChips.innerHTML = html;

    // 绑定单个移除事件
    refChips.querySelectorAll('.ai-chat-ref-chip-remove').forEach(btn => {
        btn.addEventListener('click', (e) => {
            e.stopPropagation();
            removeRefNote(btn.dataset.id);
        });
    });

    // 绑定批量移除事件
    const removeAllBtn = refChips.querySelector('.ai-chat-ref-chip-remove-all');
    if (removeAllBtn) {
        removeAllBtn.addEventListener('click', () => {
            referencedNotes = [];
            cachedRefContext = '';
            updateRefChips();
        });
    }
}
```

### 2. `frontend/src/css/components/ai-chat.css` — 批量移除标签样式

**位置**: 在 `.ai-chat-ref-chip-remove` 样式之后（约第 2109 行）

**新增样式**:

```css
/* ── 批量移除标签 ── */
.ai-chat-ref-chip-remove-all {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 3px 8px;
    border: 1px dashed var(--error, #e74c3c);
    border-radius: var(--radius-md);
    background: transparent;
    color: var(--error, #e74c3c);
    font-size: 0.72rem;
    cursor: pointer;
    white-space: nowrap;
    transition: background 0.15s, color 0.15s;
    animation: chip-enter 0.2s ease-out both;
}

.ai-chat-ref-chip-remove-all:hover {
    background: var(--error, #e74c3c);
    color: #fff;
}

.ai-chat-ref-chip-remove-all-icon {
    flex-shrink: 0;
}
```

### 3. 未受影响的部分

- `confirmNoteSelection()` 无需修改 — 批量移除只在展示层，不影响确认逻辑
- `removeRefNote()` 无需修改 — 单个移除继续可用
- HTML (`index.html`) 无需修改 — 复用现有 chips 容器
- 后端无需修改

## 交互行为

1. 引用 1-2 篇笔记：不显示批量移除标签，与原来完全一致
2. 引用 3 篇以上笔记：chips 末尾追加一个红色虚线边框的"移除全部 N 篇"标签
3. 点击"移除全部 N 篇"：清空 `referencedNotes` 和 `cachedRefContext`，重渲染 chips（bar 隐藏）
4. 手动移除部分笔记后剩余 < 3 篇：批量移除标签自动消失（因条件 `>= 3` 不再满足）

## 受影响文件

| 文件 | 变更类型 |
|------|----------|
| `frontend/src/js/ai-chat.js` | 修改 `updateRefChips()` — 追加批量移除 chip + 事件绑定 |
| `frontend/src/css/components/ai-chat.css` | 新增 `.ai-chat-ref-chip-remove-all` 样式 |

## 验证步骤

1. 引用 2 篇笔记 → 确认 → 只有单个 chips，无批量移除标签
2. 引用 3 篇笔记 → 确认 → 末尾显示"移除全部 3 篇"标签（虚线红色边框）
3. 引用 5 篇笔记 → 确认 → 末尾显示"移除全部 5 篇"标签
4. 点击"移除全部" → 所有 chips 消失，引用栏隐藏
5. 引用 3 篇后手动移除 1 篇 → 剩余 2 篇 → 批量移除标签自动消失
