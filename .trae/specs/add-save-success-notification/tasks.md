# Tasks

- [x] Task 1: 在 `createNote()` 保存成功后添加通知
  - 在 `try` 块中后端 API 调用完成后，`closeEditor()` 之前，添加 `nm.show('笔记已创建', 'success')`
  - Mock 模式（后端未绑定）时也添加通知
- [x] Task 2: 在 `updateNote()` 保存成功后添加通知
  - 在 `try` 块中后端 API 调用完成后，`closeEditor()` 之前，添加 `nm.show('笔记已更新', 'success')`
  - Mock 模式（后端未绑定）时也添加通知

# Task Dependencies

无，两个任务互不依赖，可在同一文件中完成。
