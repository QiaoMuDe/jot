# Checklist

- [x] **Task 1: constants.js** — SVG 图标已导出为 named export + window 挂载，main.js 中所有 `SVGS.*` 引用通过 import 正常绑定，编译无报错
- [x] **Task 2: notification.js** — NotificationManager 类和模拟数据已提取到独立文件，main.js import 正确，编译无报错
- [x] **全局** — `npm run build` 编译通过，271 模块转换成功，无错误
