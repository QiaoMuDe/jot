# Checklist

## 后端
- [x] `ExportAISessionAsMarkdown` 绑定签名正确，接收 `sessionID uint` 参数
- [x] 导出内容格式符合 spec：标题 + 分隔线 + 角色前缀 + reasoning_content 引用块
- [x] 保存对话框标题正确、默认文件名 `{会话标题}.md`、.md 文件过滤
- [x] 用户取消时返回"已取消"不报错
- [x] 写入文件成功时返回成功消息
- [x] 后端编译通过

## 前端 HTML/CSS
- [x] `#aiSessionContextMenu` 元素已添加到 `index.html`，含"重命名"和"导出"两个菜单项
- [x] CSS 样式正确定位、有过渡动画、`z-index` 高于侧栏（> 100）
- [x] 前端构建通过

## 前端交互逻辑
- [ ] 右键会话列表项弹出菜单，菜单位置在鼠标指针附近
- [ ] 点击菜单项后菜单关闭
- [ ] 点击菜单外区域或按 Escape 关闭菜单
- [ ] 右键菜单不影响已有的单击/双击/删除按钮交互

## 重命名
- [ ] 右键"重命名"触发的内联编辑与双击行为一致
- [ ] 改名后持久化到数据库

## 导出
- [ ] 右键"导出"弹出保存对话框
- [ ] 导出成功后显示通知
- [ ] 用户取消导出无副作用
- [ ] 导出内容包含完整对话且格式正确
