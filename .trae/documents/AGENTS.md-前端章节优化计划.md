# AGENTS.md 前端章节优化计划

## 概要

对 AGENTS.md 的前 609 行（一~九章）进行 7 项修正/优化，包括修正过时数据、删除维护成本高的行数统计表、精简 spec 目录列表、补充已实现功能、更新技术栈描述等。

---

## 修改项详情

### 修改 1：更新 L4 技术栈描述

**位置**：L4
**当前内容**：
```
> 技术栈: Wails v2 + Go + GORM + SQLite + 原生 HTML/CSS/JS + CodeMirror 6（编辑器）+ go-openai + ollama/ollama/api（AI 对话适配层）
```
**改为**：
```
> 技术栈: Wails v2 + Go + GORM + SQLite + 原生 HTML/CSS/JS + CodeMirror 6（编辑器）+ aicli 自研 AI 客户端（go-openai/Ollama 双驱动）
```
**原因**：已从 go-openai + ollama 直接调用改为自研 aicli 客户端，且 LangChainGo 已移除。

---

### 修改 2：删除 L502-L526「关键文件统计」表格

**位置**：L502-L526
**当前内容**：行数统计表格（含 20 个文件的行数数据）

**全部删除**（包括标题行 `### 关键文件统计` 和整个表格）。

**原因**：用户明确要求"不用记录哪些文件有多少行了，省得后面还得要更新"。行数会随开发动态变化，维护成本高且容易过时。

---

### 修改 3：修正目录和功能模块中的行数引用

**位置**：
- L13 `app.go ~3532` → 改为 `~3596`
- L58 `ai-chat.js ~4380` → 去掉行数描述

**原因**：行数统计要实时的最好，但不维护行数后也需要保持大致的准确。

**注意**：L58 原本是 `... ~4380 行`，此处改为不提及具体行数（类似目录中其他不标行数的模块）。L13 因为删除了统计表，可以在目录中保留行数的大致范围，但改为 `~3596 行`。

等等，既然用户说不要记录行数了，那 L13 的 `~3532 行` 和 L58 的 `~4380 行` 也应该统一去掉行数标注。保持目录简洁。

所以：
- L13: `Wails 绑定层，暴露 110+ 个 Go API 给前端，~3532 行` → `Wails 绑定层，暴露 110+ 个 Go API 给前端`
- L58: `AI 对话模块 ~4380 行（自...` → `AI 对话模块（自...`

---

### 修改 4：更新 L407 技术栈表的 AI 对话条目

**位置**：L407
**当前内容**：
```
| **AI 对话** | LangChainGo | github.com/tmc/langchaingo v0.1.14 | 统一 LLM 接口，支持 OpenAI 兼容 + Ollama 双 Provider |
```
**改为**：
```
| **AI 对话** | 自研 aicli 客户端（go-openai + ollama 双驱动） | github.com/sashabaranov/go-openai v1.41.2 + github.com/ollama/ollama v0.31.1 | 流式对话/深度思考/多会话/联网搜索/卡片召回 |
```
**原因**：LangChainGo 已从 go.mod 中移除，现使用自研 aicli 客户端，底层依赖 go-openai 和 ollama 库。

---

### 修改 5：标记 L166 "待改进" 为已解决

**位置**：L166
**当前内容**：
```
| **待改进** | frontend/dist 为构建产物，应加入 .gitignore |
```
**改为**：
```
| **待改进** | 无 |
```
（也可以直接删掉这一行，或改为 "（已解决）"）

**原因**：`.gitignore` 中已有 `frontend/dist`，问题已解决。

---

### 修改 6：删除 L88-L156 `.trae/specs/` 目录列表

**位置**：L88-L156
**当前内容**：约 70 个 spec 条目，每条一行

**改为**：**全部删除**，目录结构中 `.trae/specs/` 下仅保留一行 `└── .trae/specs/   # 项目 Spec 文档目录` 说明，不再列出具体条目。

**原因**：spec 条目会持续增多，维护列表成本高且不实用。

---

### 修改 7：补充 L540「已实现」列表

**位置**：L540-L565
**当前内容**：列出了到待办清单功能为止的已实现功能

**补充以下条目**（追加在末尾，保持字母序/按时间）：
```
- [x] **骨架屏编辑器**（点击笔记立即显示骨架屏 shimmer，异步加载内容后替换）
- [x] **搜索来源 UI 优化**（内联卡片+折叠面板+SVG 图标+代码去重）
- [x] **召回卡片 UI 优化**（折叠面板+file_ext 徽章+CSS line-clamp+代码去重）
- [x] **编辑器骨架屏回归修复**（非缓存笔记打开校正+scrollbar-gutter 稳定）
- [x] **品牌标识动画优化**（transform 独立驱动，3 次迭代达成平滑过渡）
- [x] **用户消息 Token 提前展示**（SaveAIMessage 返回 token 数，立即显示）
- [x] **停止按钮全阶段防护**（搜索/LLM 阶段取消不报错不残留）
- [x] **Logger 初始化顺序修复**（NewApp 阶段初始化 Logger，startup 清理冗余代码）
```

**原因**：最近 10 个记忆点中的 8 个功能改进应该反映在"已实现"列表中。

---

## 验证方式

修改完成后，通过 Grep 搜索 `~[0-9]` 确认所有行数引用都已清理，检查文件整体结构是否保持连贯。