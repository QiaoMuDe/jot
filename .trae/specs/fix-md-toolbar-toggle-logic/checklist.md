# 验证清单

- [x] 标题下拉面板背景不透明，在所有 6 个主题下正确显示
- [x] 标题下拉面板有 1px 边框、圆角、阴影
- [x] 标题下拉选项 hover 时左侧 accent 竖条可见
- [x] 点击下拉外部面板关闭
- [x] 加粗：无选中插入 `****` 光标居中
- [x] 加粗：选中 `**text**` 时去除包裹保留 `text`
- [x] 加粗：选中 `text` 时添加 `**text**`
- [x] 斜体：无选中插入 `**` 光标居中
- [x] 斜体：选中 `*text*` 时去除包裹保留 `text`
- [x] 斜体：选中 `text` 时添加 `*text*`
- [x] 删除线：无选中插入 `~~~~` 光标居中
- [x] 删除线：选中 `~~text~~` 时去除包裹保留 `text`
- [x] 删除线：选中 `text` 时添加 `~~text~~`
- [x] 行内代码：无选中插入 `` `` `` 光标居中
- [x] 行内代码：选中 `` `text` `` 时去除包裹保留 `text`
- [x] 行内代码：选中 `text` 时添加 `` `text` ``
- [x] 格式化后自动 `cmEditor.focus()` 保持编辑器焦点
- [x] 构建通过（`npm run build` 无错误）
