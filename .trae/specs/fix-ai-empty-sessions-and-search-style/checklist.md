# Checklist

- [x] 联网搜索下拉菜单使用 `.open` class 控制显隐，不再直接操作 `style.display`
- [x] 联网搜索下拉菜单样式（背景、阴影、圆角、过渡动画）与"更多技能"菜单一致
- [x] 点击联网搜索按钮，菜单展开时菜单项逐个滑入
- [x] 点击外部区域关闭联网搜索下拉菜单
- [x] 点击内容为空的会话，会话项高亮状态正确切换
- [x] 切换空会话时，标题和欢迎页正确显示
- [x] 数据库瘦身后端新增 `DeleteEmptyAISessions()` 方法，删除无关联消息的 AI 会话
- [x] `VacuumDatabase()` 在执行 VACUUM 前调用删除空会话逻辑
- [x] 数据库瘦身结果提示中包含清理的空会话数量
- [x] 侧边栏从折叠展开时，调用 `loadSessionList()` 刷新列表
- [x] 侧边栏展开后看到的是数据库中的最新会话列表
- [x] `wails build -s` 编译通过
- [x] `npx vite build` 前端构建通过
