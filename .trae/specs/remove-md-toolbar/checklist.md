# 验证清单

- [x] `index.html` 中 `#editorToolbar` 整个块已删除（第 157-202 行）
- [x] `editor.css` 中所有工具栏相关 CSS 已删除（约 130 行）
- [x] `editor.css` 中 `.editor-panes` 布局保持正常
- [x] `main.js` 中 10 个 `format*()` 函数已删除（直接编辑确认）
- [x] `main.js` 中 `initEditorToolbar()` 及调用已删除（直接编辑确认）
- [x] `main.js` 中 CM6 `keymap.of()` 内 Ctrl+B/I/U 已移除（子代理完成）
- [x] `main.js` 中 `els` 对象中工具栏 DOM 引用已删除（直接编辑确认）
- [x] `index.html` 中 `mdToolbarToggle` 设置行已删除（子代理完成）
- [x] `main.js` 中 `loadToolbarSetting()` 函数及调用已删除（直接编辑确认）
- [x] `main.js` 中 `mdToolbarToggle` change 事件监听已删除（直接编辑确认）
- [x] `main.js` 中 `els.mdToolbarToggle` 引用已删除（直接编辑确认）
- [x] `main.js` 中 `openEditor()` 内的工具栏显隐控制已删除（子代理完成）
- [x] `main.js` 中类型切换 handler 内的工具栏显隐控制已删除（直接编辑确认）
- [x] `main.js` 中 `switchToMarkdownMode()` 内的工具栏显隐控制已删除（子代理完成）
- [x] 编辑器区域布局正常，MD 笔记可以正常编辑（构建通过）
- [x] 设置页无"格式化工具栏"相关项残留
- [x] `npm run build` 通过
