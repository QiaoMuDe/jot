# Checkpoints

- [x] 后端 `SearchNoteIDs` API 实现，支持 keyword / notebookID / tagIDs 组合筛选返回全部 ID
- [x] 全选按钮 DOM 在浮层底部 footer 区域正确显示（计数文字左侧）
- [x] 全选按钮样式（inline-flex + 复选框图标），选中/未选中态视觉效果正确
- [x] 点击全选 → 全选当前筛选条件下的所有笔记（底部计数更新，条目显示选中态）
- [x] 再次点击全选 → 取消全选（所有条目取消选中，计数归零）
- [x] 全选后滚动加载更多 → 新追加条目自动选中
- [x] 全选状态下手动取消某一条 → 全选按钮自动取消勾选
- [x] 手动逐条选中所有条目 → 全选按钮自动变为勾选状态
- [x] 全选激活后切换筛选（笔记本/标签/搜索）→ 全选按钮重置，但已有选中不清除
- [x] 全选后确认引用 → 正确携带所有选中笔记 ID 到后端
