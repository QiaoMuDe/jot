# Checkpoints

- [x] Task 1: Delete animation plays after confirming deletion
  - Open settings → 配置预设 → 管理 (create 5+ presets)
  - Click "删除" on any row, confirm the dialog
  - Verify row plays slide-out animation before disappearing
  - Verify API call works and list re-renders correctly
- [x] Task 2: Scrollbar persists in preset manager list when content overflows
  - Create 10+ presets, open the manager list
  - Verify vertical scrollbar is visible on the right side
  - Scroll the container, verify scrollbar remains visible during and after scrolling
  - Verify all rows (including those below the fold) are accessible via scrolling
