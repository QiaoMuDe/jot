# Check List

- [ ] 后端 `Search()` 和 `SearchByNotebook()` 支持 `scope` 筛选参数（title/content/all）
- [ ] 后端 `Search()` 和 `SearchByNotebook()` 支持 `days` 时间范围筛选参数
- [ ] 后端 `Search()` 和 `SearchByNotebook()` 支持 `tagID` 标签筛选参数
- [ ] 后端 `GetNotebookTags()` API 实现并暴露给前端
- [ ] 搜索视图 HTML 包含三个筛选下拉和重置按钮
- [ ] 筛选栏样式与温暖极简主题一致（紧凑、优雅）
- [ ] 搜索范围下拉默认选中"全部"，选项包含"全部/标题/内容"
- [ ] 时间筛选下拉默认选中"全部时间"，选项包含"全部时间/最近7天/最近30天/最近90天/最近1年"
- [ ] 标签筛选下拉默认选中"全部标签"，且选项从后端动态加载
- [ ] 标签下拉中每个标签项显示颜色圆点和标签名称
- [ ] 切换任一筛选条件后自动重新搜索（带300ms防抖）
- [ ] 重置按钮一键重置所有筛选为默认值并重新搜索
- [ ] 切换笔记本时自动重置所有筛选为默认值
- [ ] 退出搜索视图时重置所有筛选为默认值
- [ ] 后端 API 绑定重新生成（wails generate module）
- [ ] 搜索功能整体正常：无筛选时行为与原来一致