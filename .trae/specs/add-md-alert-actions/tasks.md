# Tasks

- [x] Task 1: 在 `md-syntax.js` 的 `块元素` 子菜单中添加 5 个 Alert 插入操作项
  - 在现有`引用`操作项之后添加
  - 每个操作项处理无选中文本（插入模板）和有选中文本（包裹）两种场景
  - 无选中时插入 `\n> [!TYPE]\n> 提示内容\n`，选中时插入 `\n> [!TYPE]\n> 选中文本\n`

## Task Dependencies

无