# 导入笔记携带图片方案

## 摘要

补全导入逻辑，实现导入/导出图片闭环：导入 `.md` 文件时，扫描内容中的图片语法，将引用的本地图片文件复制到 `~/.jot/images/`（文件服务器），并把引用改写为 `/images/xxx` 内部 URL。互联网 URL 与缺失的图片保留原样。仅处理 `.md` 类型。

## 现状分析

- 导入入口：`ImportFiles`（[app.go](file:///d:/峡谷/Dev/本地项目/jot/app.go#L3884)）→ `processImportFile`（[app.go](file:///d:/峡谷/Dev/本地项目/jot/app.go#L3958-L4132)）处理单个文件。
- `processImportFile` 流程：stat → 拒绝目录 → 大小限制 → 标题/后缀 → 内容读取（办公文件转 `.md` / 纯文本直读）→ 哈希去重对比（[L4045-L4071](file:///d:/峡谷/Dev/本地项目/jot/app.go#L4045-L4071)）→ 冲突/覆盖/创建。
- 现有可复用函数：`SaveImageFromPath`（[app.go](file:///d:/峡谷/Dev/本地项目/jot/app.go#L340-L367)）从本地路径复制图片到 `~/.jot/images/` 并返回 `/images/uuid_原名` URL，正是导入复制所需。
- 冲突覆盖分支 `ResolveImportConflict`（[L4138](file:///d:/峡谷/Dev/本地项目/jot/app.go#L4138)）接收前端回传的 content，其值源自 `result.Content`（已处理内容），无需改动。
- 导出侧已实现：`ExportNoteAsMarkdown`（[app.go](file:///d:/峡谷/Dev/本地项目/jot/app.go#L3270)）导出 `xxx.md + xxx.md.assets/` 相对引用，本方案使该产物可被导入还原。
- imports 现状：`regexp`、`strings`、`filepath`、`os`、`config` 均已导入；本方案不引入新依赖（URL 判断用字符串前缀，不用 `net/url`）。

## 改动方案

仅修改 [app.go](file:///d:/峡谷/Dev/本地项目/jot/app.go)，分两处。

### 1. 新增辅助函数 `processImportImages`

位置：`importContentHash`（[L3949](file:///d:/峡谷/Dev/本地项目/jot/app.go#L3949)）之前。

```go
// processImportImages 处理导入内容中的本地图片引用：把存在的本地图片复制到
// ~/.jot/images/ 并将引用改写为 /images/xxx 内部 URL。
// baseDir 为导入文件所在目录，用于解析相对路径。
// 互联网 URL（含 data:）与缺失图片保留原样。
func (a *App) processImportImages(content string, baseDir string) string {
	imgRe := regexp.MustCompile(`!\[[^\]]*\]\(([^)]+)\)`)
	cache := make(map[string]string) // 已复制的本地路径 → /images/xxx，同路径去重
	return imgRe.ReplaceAllStringFunc(content, func(s string) string {
		m := imgRe.FindStringSubmatch(s)
		raw := m[1] // 可能是 `path "title"` 或 `path`
		pathPart := raw
		if idx := strings.Index(raw, `"`); idx > 0 {
			pathPart = strings.TrimSpace(raw[:idx]) // 拆分可选的 "title" 后缀，路径允许含空格
		}
		// 互联网 URL / data URI → 保留原样
		if strings.Contains(pathPart, "://") || strings.HasPrefix(pathPart, "data:") {
			return s
		}
		// 已是 jot 内部引用 → 保留（幂等，避免重复处理）
		if strings.HasPrefix(pathPart, "/images/") {
			return s
		}
		// 解析为本地路径：绝对路径直接用，相对路径基于导入文件所在目录
		localPath := pathPart
		if !filepath.IsAbs(localPath) {
			localPath = filepath.Join(baseDir, localPath)
		}
		localPath = filepath.Clean(localPath)
		// 图片不存在 → 保留原引用
		info, err := os.Stat(localPath)
		if err != nil || info.IsDir() {
			a.LogSvc.Logger.Debugw("processImportImages: 图片不存在，保留原引用",
				fastlog.String("path", pathPart), fastlog.Error(err))
			return s
		}
		// 复制到文件服务器（同路径只复制一次）
		newURL, ok := cache[localPath]
		if !ok {
			newURL, err = a.SaveImageFromPath(localPath)
			if err != nil {
				a.LogSvc.Logger.Warnw("processImportImages: 复制图片失败", fastlog.String("path", pathPart), fastlog.Error(err))
				return s
			}
			cache[localPath] = newURL
		}
		// 只替换路径部分，保留 alt 与 title
		return strings.Replace(s, pathPart, newURL, 1)
	})
}
```

### 2. `processImportFile` 中调用

在内容读取完成（[L4041](file:///d:/峡谷/Dev/本地项目/jot/app.go#L4041) `content = string(data)` 汇合点）之后、查找已有笔记（[L4045](file:///d:/峡谷/Dev/本地项目/jot/app.go#L4045)）之前插入：

```go
// 4.5 处理 .md 内容中的本地图片引用：存在则复制到文件服务器并改写引用
if fileExt == ".md" && strings.Contains(content, "![") {
	content = a.processImportImages(content, filepath.Dir(path))
}
```

处理后的 `content` 供后续哈希对比、冲突、覆盖、创建统一使用。

## 假设与决策

- **URL 判定**：含 `://` 或以 `data:` 开头视为互联网 URL 跳过，不引入 `net/url`。Windows 绝对路径 `C:/...` 不含 `://`，会被正确当本地路径处理。
- **幂等**：`/images/xxx` 引用直接跳过，处理后的内容再次导入不再复制 → 文件哈希对比稳定，不会误判。
- **缺失图片**：保留原引用，仅记 Debug 日志，导入不中断（用户已确认）。
- **处理范围**：仅 `fileExt == ".md"`（含办公文件转 `.md`），`.txt` 不处理（用户已确认）。
- **不做 URL 解码**：路径中的 `%20` 等编码不还原；若编码后文件不存在则保留引用（少见场景，保持简单）。
- **title 语法**：`![alt](path "title")` 保留 title，仅替换 path。
- **不新增结果字段**：图片复制数量不加入 `FileImportResult`，仅日志记录，保持改动最小。

## 验证步骤

1. `go build ./...` 编译通过。
2. 手动验证（拖拽/导入一个 `.md`）：
   - 内容含 `![](相对路径/图.png)` 且文件存在 → 图片出现在 `~/.jot/images/`，引用变为 `/images/uuid_图.png`，预览显示正常。
   - 内容含 `![](https://example.com/a.png)` → 引用原样保留。
   - 内容含 `![](不存在.png)` → 引用保留，导入成功。
   - `.txt` 文件含图片语法 → 内容原样，不处理。
3. 导出→导入闭环：导出含图笔记（产生 `xxx.md + xxx.md.assets/`）→ 重新导入该 `xxx.md` → 图片复制到 `~/.jot/images/`，引用变为内部 URL，预览显示正常。
4. 幂等验证：对同一文件连续导入两次 → 第二次因内容哈希一致显示 skipped，不再重复复制图片。
