# Checklist

## 后端
- [x] `SaveAIMessageAsNote` 绑定签名正确，接收 `content string`
- [x] 空内容时返回错误
- [x] 标题自动生成正确：第一行/首段，截断到 50 字符
- [x] 使用 `.md` 作为文件后缀
- [x] 创建成功返回笔记对象
- [x] 后端编译通过

## 前端
- [x] "保存为笔记"按钮出现在 AI 回复气泡的悬浮操作区（复制和重新生成之间）
- [x] 用户消息气泡上没有"保存为笔记"按钮
- [x] 点击按钮时传入的是纯回复内容（不含 reasoning_content）
- [x] 保存成功后显示通知 "笔记已创建"
- [~] ~~"查看"按钮~~ — `showNotification` 不支持按钮数组，暂不实现（后续可扩展 notification 组件后补充）
- [x] 保存失败时显示错误通知
- [x] 前端构建通过
