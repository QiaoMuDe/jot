# Tasks

- [x] Task 1: 为 `.md-rendered` 增加 `scroll-padding-top` CSS 属性
  - [x] 在 `frontend/src/css/components/editor.css` 的 `.md-rendered` 规则块中，新增 `scroll-padding-top: 0.75rem`
  - [x] 该属性确保 `scrollIntoView({ block: 'start' })` 或锚点跳转时，标题顶部有 `0.75rem` 的留白空间，与预览区自身的 `padding-top` 保持一致

# Task Dependencies

- 无依赖，单文件单行 CSS 修改，可直接实施
